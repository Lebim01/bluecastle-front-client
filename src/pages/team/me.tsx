import DirectUsers from "@/components/views/direct-users";
import MainLayout from "@/layouts/main";

const DirectMe = () => {
  return (
    <MainLayout>
      <DirectUsers />
    </MainLayout>
  );
};

export default DirectMe;
